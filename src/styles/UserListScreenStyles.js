import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    height: '100%',
  },
  itemStyle: {
    width: '100%'
  },
  title: {
    fontSize: 32,
  },
});

export default styles;
